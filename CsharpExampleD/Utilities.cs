namespace CsharpExampleD
{
    public class Utilities
    {
        // If the target framework supports multiple OS
        // This method should print the operating system for which
        // the dll was compiled for. It must do this using the #if directive
        // Otherwise it should always return "Windows"
        public static string GetCompiledOperatingSystem()
        {
            throw new NotImplementedException();
        }
    }
}